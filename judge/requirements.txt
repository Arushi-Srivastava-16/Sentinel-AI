# =============================================================================
# Judge module — LLM dependencies
# Python 3.12 required
#
# These are a subset of gateway/requirements.txt extracted so the judge
# module can be installed/tested independently (e.g. in a separate K8s job).
# =============================================================================

# Anthropic API (Tier 3 — Claude Haiku fallback)
anthropic==0.28.0

# OpenAI-compatible client (used by instructor to talk to Ollama)
openai==1.30.1

# Structured LLM output — eliminates JSON parse failures
instructor==1.3.3

# Jinja2 for prompt templates
jinja2==3.1.4

# LLM observability
langfuse==2.36.1

# Async HTTP (shared with gateway)
httpx==0.27.0

# Structured logging
structlog==24.1.0
